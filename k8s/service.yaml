apiVersion: v1
kind: Service
metadata:
  name: fam-app-be
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  selector:
    app: fam-app-be
  ports:
    - port: 80
      targetPort: 3000
  type: LoadBalancer
